import type * as Vapi from "../index.js";
export interface TestSuiteTestScorerAi {
    /** This is the type of the scorer, which must be AI. */
    type: Vapi.TestSuiteTestScorerAiType;
    /** This is the rubric used by the AI scorer. */
    rubric: string;
}
