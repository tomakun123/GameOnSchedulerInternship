import type * as Vapi from "../index.js";
export interface ChatCost {
    /** This is the type of cost, always 'chat' for this class. */
    type: Vapi.ChatCostType;
    /** This is the cost of the component in USD. */
    cost: number;
}
