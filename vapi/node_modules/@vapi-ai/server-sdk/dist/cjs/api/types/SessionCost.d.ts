import type * as Vapi from "../index.js";
export interface SessionCost {
    /** This is the type of cost, always 'session' for this class. */
    type: Vapi.SessionCostType;
    /** This is the cost of the component in USD. */
    cost: number;
}
