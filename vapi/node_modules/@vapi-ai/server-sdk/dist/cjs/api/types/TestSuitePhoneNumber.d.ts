import type * as Vapi from "../index.js";
export interface TestSuitePhoneNumber {
    /** This is the provider of the phone number. */
    provider: Vapi.TestSuitePhoneNumberProvider;
    /** This is the phone number that is being tested. */
    number: string;
}
